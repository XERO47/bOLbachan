# ── DO NOT add torch / torchvision / torchaudio here ──────────────────────────
# The NGC base image (nvcr.io/nvidia/pytorch:23.07-py3) already ships
# PyTorch 2.1.0 + CUDA 12.1 compiled with cuDNN/NCCL optimisations.
# Reinstalling would downgrade to a non-optimised wheel and break CUDA paths.
# ──────────────────────────────────────────────────────────────────────────────

# Web server
fastapi==0.110.0
uvicorn[standard]==0.27.1
websockets==12.0
python-multipart==0.0.9

# Video / image
opencv-python-headless==4.8.1.78
Pillow==10.2.0

# Audio
soundfile==0.12.1
librosa==0.10.1
# pyaudio is only needed for local mic capture; not required on the server

# Numpy — stay on 1.x; numpy 2.x breaks many CUDA-era libs still on PyTorch 2.1
numpy==1.26.4
scipy==1.12.0

# Face detection
mediapipe==0.10.11
insightface==0.7.3
onnxruntime-gpu==1.17.1    # must match CUDA 12.1; 1.17 is correct

# MuseTalk / diffusion stack
# Pin to versions tested against torch 2.1.0
einops==0.7.0
diffusers==0.25.1          # 0.27+ requires torch ≥ 2.2
transformers==4.37.2
accelerate==0.26.1
huggingface_hub==0.21.4
safetensors==0.4.2
omegaconf==2.3.0

# Utilities
pydantic==2.6.3
pydantic-settings==2.2.1
aiofiles==23.2.1
psutil==5.9.8
prometheus-client==0.20.0
